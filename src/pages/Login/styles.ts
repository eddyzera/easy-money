import { styled } from '../../stitches.config'

export const LoginContainer = styled('div', {
  height: '100vh',
  width: '100%',
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'center',
})